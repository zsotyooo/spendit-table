{
  "name": "@spendit/root",
  "private": true,
  "scripts": {
    "build": "FORCE_COLOR=1 turbo run build",
    "dev": "FORCE_COLOR=1 turbo run dev",
    "lint": "FORCE_COLOR=1 turbo run lint",
    "lint:fix": "FORCE_COLOR=1 turbo run lint:fix",
    "test": "FORCE_COLOR=1 turbo run test",
    "test:dev": "FORCE_COLOR=1 turbo run test:dev"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "playground"
    ]
  },
  "devDependencies": {
    "turbo": "^1.7.0"
  }
}
